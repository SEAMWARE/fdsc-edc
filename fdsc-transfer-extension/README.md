# FDSC Transfer Extension

Extension for the EDC, to enable transfer processes through the FIWARE Dataspace Connector. It currently only supports HTTP-Pull based transfers, using OID4VC.

* manages the required routes in 
* generates the configuration required for authentication in the Credentials-Config-Service
* manages the policies to be applied for the process at the ODRL-PAP