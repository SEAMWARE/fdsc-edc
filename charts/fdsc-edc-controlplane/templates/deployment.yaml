apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "edc.fullname" . }}-controlplane
  namespace: {{ $.Release.Namespace | quote }}
  labels:
    app.kubernetes.io/name: {{ include "edc.name" . }}-dataplane
    {{ include "edc.labels" . | nindent 4 }}
spec:
  {{- if not .Values.autoscaling.enabled }}
  replicas: {{ .Values.deployment.replicaCount }}
  {{- end }}
  revisionHistoryLimit: {{ .Values.deployment.revisionHistoryLimit }}
  strategy:
  {{- with .Values.deployment.updateStrategy }}
  {{- toYaml . | nindent 4 }}
  {{- end }}
  selector:
    matchLabels:
      app.kubernetes.io/name: {{ include "edc.name" . }}-controlplane
      app.kubernetes.io/instance: {{ .Release.Name }}
  template:
    metadata:
      labels:
        app.kubernetes.io/name: {{ include "edc.name" . }}-controlplane
        {{ include "edc.labels" . | nindent 8 }}
        {{- with .Values.deployment.additionalLabels }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
      {{- if .Values.deployment.additionalAnnotations -}}
      annotations:
        {{- with .Values.deployment.additionalAnnotations }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
      {{- end }}
    spec:
      {{- if .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- range .Values.imagePullSecrets }}
        - name: {{ . }}
        {{- end }}
      {{- end }}
      serviceAccountName: {{ include "edc.serviceAccountName" . }}
      {{- if .Values.deployment.initContainers }}
      initContainers:
        {{- with .Values.deployment.initContainers }}
          {{- toYaml . | nindent 8 }}
          {{- end }}
      {{- end }}
      hostAliases:
        - ip: "192.168.2.224"  # Replace with your host's IP
          hostnames:
            - "local-host"
      containers:
        - name: {{ .Chart.Name }}
          imagePullPolicy: {{ .Values.deployment.image.pullPolicy }}
          image: "{{ .Values.deployment.image.repository }}:{{ .Values.deployment.image.tag }}"
          {{- if .Values.deployment.command }}
          command:
          {{- with .Values.deployment.command }}
          {{- toYaml . | nindent 12 }}
          {{- end }}
          {{- end }}
          {{- if .Values.deployment.args }}
          args:
            - --log-level=debug
          {{- with .Values.deployment.args }}
          {{- toYaml . | nindent 12 }}
          {{- end }}
          {{- end }}
          ports:
            - name: http
              containerPort: {{ .Values.deployment.port}}
              protocol: TCP
            - name: http-version
              containerPort: {{ .Values.deployment.version.port}}
              protocol: TCP
            - name: http-management
              containerPort: {{ .Values.deployment.management.port}}
              protocol: TCP
            - name: http-protocol
              containerPort: {{ .Values.deployment.protocol.port}}
              protocol: TCP
            - name: http-catalog
              containerPort: {{ .Values.deployment.catalog.port}}
              protocol: TCP
            - name: http-control
              containerPort: {{ .Values.deployment.control.port}}
              protocol: TCP
            - name: http-tck
              containerPort: 8687
              protocol: TCP
          livenessProbe:
            httpGet:
              path: {{ .Values.deployment.path}}/check/liveness
              port: http
            initialDelaySeconds: {{ .Values.deployment.livenessProbe.initialDelaySeconds }}
            periodSeconds: {{ .Values.deployment.livenessProbe.periodSeconds }}
            successThreshold: {{ .Values.deployment.livenessProbe.successThreshold }}
            timeoutSeconds: {{ .Values.deployment.livenessProbe.timeoutSeconds }}
          readinessProbe:
            httpGet:
              path: {{ .Values.deployment.path}}/check/readiness
              port: http
            initialDelaySeconds: {{ .Values.deployment.readinessProbe.initialDelaySeconds }}
            periodSeconds: {{ .Values.deployment.readinessProbe.periodSeconds }}
            successThreshold: {{ .Values.deployment.readinessProbe.successThreshold }}
            timeoutSeconds: {{ .Values.deployment.readinessProbe.timeoutSeconds }}
          env:
            - name: EDC_FS_CONFIG
              value: /config/dataspaceconnector-configuration.properties
            {{- with .Values.additionalEnvVars }}
            {{- toYaml . | nindent 12 }}
            {{- end }}
          volumeMounts:
            - name: config-properties
              mountPath: /config/dataspaceconnector-configuration.properties
              subPath: dataspaceconnector-configuration.properties
            {{- if .Values.deployment.additionalVolumeMounts }}
            {{- with .Values.deployment.additionalVolumeMounts }}
            {{- toYaml . | nindent 12 }}
            {{- end }}
            {{- end }}
          resources:
            {{- toYaml .Values.deployment.resources | nindent 12 }}
      volumes:
        - name: config-properties
          configMap:
            name: {{ include "edc.fullname" . }}-controlplane
        {{- if .Values.deployment.additionalVolumes }}
        {{- with .Values.deployment.additionalVolumes }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
        {{- end }}
      {{- with .Values.deployment.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.deployment.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.deployment.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}